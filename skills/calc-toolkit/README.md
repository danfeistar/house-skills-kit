# ⑦ 公共计算工具库（打样第一批 · 13/20）

> 全国市场通用 ｜ 数字绝对精确、公式透明可验 ｜ 规则三层可编辑（08-29 定盘铁律）

## 快速上手（零依赖，任何电脑有 Python3 即可跑）

```bash
python3 skills/calc-toolkit/mortgage.py --price 1500000 --downpay 30 --years 30 --city 昆明
python3 skills/calc-toolkit/tax.py --price 1500000 --area 89 --city 昆明 --first 1 --held 6 --unique 1   # 全成本：税4项+费6项，买卖分账
python3 skills/calc-toolkit/prepay.py --loan 1000000 --rate 3.1 --years 30 --paid 24 --prepay 200000    # 提前还款：缩期vs减月供对比
python3 skills/calc-toolkit/capacity.py --income 8000,6000 --cash 400000 --debts 2000 --city 昆明   # 购房能力：收入负债→可买总价
python3 skills/calc-toolkit/newhome.py --price 1500000 --area 100 --city 昆明 --first 1                 # 一手房：契税+维修基金+首年物业+配套
python3 skills/calc-toolkit/floor.py --price 25000 --area 100 --floor 15 --top 30 --floor-adj 300 --d 98 --d 99 --pay one_time
```

## 十三件工具

| # | 工具 | 脚本 | 覆盖场景 |
|---|---|---|---|
| ⑦-1 | 房贷月供计算器 | `mortgage.py` | 商贷/公积金/组合贷 × 等额本息/本金，利息对比、首付档校验 |
| ⑦-2 | 二手房税费计算器 | `tax.py` | 契税（首二套×面积段）/增值税（满二）/个税（满五唯一、差额取低），买卖双方分担 |
| ⑥ | 首套/二套认定器 | `status.py` | 商贷/公积金/契税三口径分别判定（认房不认贷 vs 认房又认贷、卖一买一、公积金次数），结果直通税费/首付/月供工具 |
| ⑦-3 | 提前还款测算器 | `prepay.py` | 缩期 vs 减月供双方案对比、违约金提示（规则库可配）、最低还款额提示、结清场景识别 |
| ⑦-5 | 购房能力测算器 | `capacity.py` | 银行双线口径（月供收入比×首付成数）→可买总价上限、瓶颈诊断、推荐看房区间、改善通道 |
| ⑦-6 | 一手房成本测算器 | `newhome.py` | 契税/维修基金（按城市×物业类型三档）/首年物业/登记工本费/贷款杂费，五类分组 |
| ⑦-15 | 楼层差价+折扣叠加 | `floor.py` | 楼层加价 × 营销折上折 × 付款折扣，**折扣真伪校验**（防"先涨后折"）、邻层速查 |
| ⑦-16 | 公积金贷款额度测算器 | `fund.py` | 四限取最低（上限封顶/房价成数/还贷能力/余额倍数×缴存时间系数），多子女/绿建/人才上浮；昆明=2026-04-07新政（单70/双100，叠加至150万） |
| ⑦-17 | 佣金/提成试算器 | `commission.py` | 二手中介费买卖分账+活动折扣；渠道阶梯**双算法对账**（全额跳档vs分段累进）；返佣+团队分佣一条龙 |
| ⑦-18 | 得房率与单价换算器 | `efficiency.py` | 建面↔套内↔公摊↔使用面积链+反推；单价双向换算；双盘套内口径比价照妖镜 |
| ⑦-19 | 租金回报率计算器 | `rent.py` | 租售比/毛净回报率/回收年限；五档坐标（价值线/回本线/50城均值/存款/以租养贷）；目标回报率反推 |
| ⑦-20 | LPR变动影响测算器 | `lpr.py` | 重定价月供变化（合同加点不变）；情景沙盘±10/25/50BP；重定价周期3/6/12选择（存续期仅改一次） |
| ⑦-21 | 持有成本测算器 | `holding.py` | 自住全成本（物业/取暖/资金占用隐性成本）；出租净现金流（空置+中介费+综合征收率）；5年累计 |

## 规则三层引擎（核心设计）

```
层1 全国默认档（rules/cities.yaml → default）
      ↓ 被覆盖
层2 城市档（cities: 昆明/成都/武汉…35城）→ 区县档（districts）
      ↓ 被覆盖
层3 我的模板（--save-template 存 JSON，--template 调用）
```

- **未内置城市**：自动套全国默认档并明确提示（同级模板+一键改，不装懂）
- **每个数字可追溯**：输出末尾附「参数来源表」——这个利率来自哪个城市档、哪个版本时点
- **改政策不改代码**：长沙契税新政出台 → 只改 `rules/cities.yaml` 一行，工具秒更新

```bash
# 存模板：把昆明参数存成我的模板
python3 tools/mortgage.py --city 昆明 --save-template my_kunming --price 1500000 --downpay 30 --years 30
# 之后任何城市参数不满足时：
python3 tools/mortgage.py --template my_kunming --price 1800000 --downpay 25 --years 25
```

## 老何验收指南（手工复核，10分钟）

**① 月供**：`--loan 1050000 --years 30 --city 昆明`（3.10%）
- 等额本息月供应 ≈ 4,484 元。计算器验算：月利率 = 0.031/12 = 0.0025833，360 期 → 月供 = 105万×0.0025833×(1.0025833)^360÷((1.0025833)^360−1)
- 等额本金首月 = 105万/360 + 105万×0.0025833 = 2,916.67 + 2,712.50 = **5,629 元** ✓
- 两方案利息差 74,516 元：0.031×105万×(359+1)/2×(月) 差额推导

**② 税费**：昆明 150 万 / 89㎡ / 首套 / 满五唯一 → 只有契税 1%（≤90㎡）= **15,000 元**
- 对照：武汉 200 万 / 120㎡ / 二套 / 未满二 → 契税 2%=4 万 + 增值税 5%=10 万 + 个税 1%=2 万 = **16 万**

**③ 折扣**：25,300 元/㎡ × 100㎡ = 253 万 → 98折 × 99折 × 一次性让2个点
- 综合 = 0.98×0.99×0.98 = **0.9508**，成交 = 253万 × 0.9508 = **2,405,514 元**（优惠 124,486）
- 套路校验：把基准改 26,000 同样折扣 → 折后更高 → 判定"折扣注水"

## 已知边界（诚实声明）

- 规则库全部档位为**演示口径**（yaml 顶部已声明），正式商用前须逐城核对当地最新官方公告——这是产品机制（数据外置+版本标注），不是缺陷
- 楼层差按"每层递增 X 元/㎡"线性简化；一房一价精确口径以销控表为准
- 个税"差额20% vs 全额1%"演示取低逻辑，部分城市强制差额法 → 改城市 YAML 即可
- 契税/增值税以家庭为单位认定（含未成年子女），工具按用户自报 `--first` 计算

## 后续 17 件（按锁版清单推进）

提前还款③ / 新房成本④ / 购房能力⑤ / 首套二套认定⑥ / 公积金额度⑦ / 佣金提成⑧ / 得房率⑨ / 租金回报⑩ / LPR影响⑪ / 持有成本⑫ / 买房vs理财⑬ / 分销核对⑭ / 面积误差⑯ / 违约定金⑰ / 房龄年限⑱ / 楼面价货值⑲ / 日照间距⑳
